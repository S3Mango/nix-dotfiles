{ lib, config, pkgs, inputs, ...}: {
  programs.kitty = {
    enable = true;
    settings = {
      confirm_os_window_close = 0;
      background_opacity = "0.6";
      allow_remote_control = true;
    };
  };
}
